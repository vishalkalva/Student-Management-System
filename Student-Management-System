sms=[]
def search_student():
        found = False
        entered_id = int(input("Enter student ID: "))

        for student in sms:
                if student["id"] == entered_id:
                    print(student)
                    found = True
                    break

        if not found:
            print("Student not found")
def update_student():
    found = False
    entered_id = int(input("Enter student ID to update: "))

    for student in sms:
        if student["id"] == entered_id:
            student["student"] = input("Enter new student name: ")
            student["age"] = int(input("Enter new student age: "))
            student["marks"] = int(input("Enter new student marks: "))

            print("Student updated successfully")
            found = True
            break

    if not found:
        print("Student not found")            
def delete_student():
    found = False
    entered_id = int(input("Enter student ID to delete: "))

    for student in sms:
        if student["id"] == entered_id:
            sms.remove(student)
            print("Student deleted successfully")
            found = True
            break

    if not found:
        print("Student not found")           
while True:
    print("\n1. Add Student")
    print("2. View Students")
    print("3. Search Student")
    print("4. Update Student")
    print("5. Delete Student")
    print("6. Exit")
    choice = int(input("Enter your choice: "))

    if(choice==1):
        id=int(input("enter student id:"))    
        student=input("enter student name:")
        age=int(input("enter student age:"))  
        marks= int(input("enter student marks:"))
        record={}
        record.update({"id":id})
        record.update({"student":student})
        record.update({"age":age})
        record.update({"marks":marks})
        sms.append(record)
        print(sms)
    elif(choice==2):
        print(sms)
        for student in sms:
            print(student)
    elif(choice==3):
        search_student()
    
    elif(choice == 4):
      update_student()  
    elif(choice == 5):
      delete_student() 
    elif(choice==6):
        print("thank you ")
        break
    else:
        print("Invalid choice! Please enter a number between 1 and 6.")
